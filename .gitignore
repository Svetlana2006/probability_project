# Browser Profile (contains secrets and locked files)
.browser-profile/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Matplotlib cache
.matplotlib/

# Output Data & Reports
# output/ is now tracked so figures, tables, and report.md are visible on GitHub.
# Only skip the template csv (it regenerates automatically) and error log.
output/genre_mapping_template.csv
pipeline_err.txt

# Personal Workbooks (optional - remove if you want to track the excel)
# *.xlsx
